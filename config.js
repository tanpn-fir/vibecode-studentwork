/* ===========================================================================
   CẤU HÌNH
   ⚠️ BÀI TẬP 6: API key dưới đây đang bị "hardcode" (ghi cứng) trong code.
   Như vậy là KHÔNG an toàn — ai xem code cũng thấy key, có thể bị lạm dụng.

   Nhiệm vụ: nhờ AI chuyển key này ra file .env, thêm .env vào .gitignore,
   rồi khai báo lại trong Environment Variables trên Vercel.
   (Đây là key giả định để luyện tập, không phải key thật.)
   =========================================================================== */

const API_KEY = "sk-demo-PUBLIC-1234567890-DO-NOT-COMMIT";
