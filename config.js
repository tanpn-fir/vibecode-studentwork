/* ===========================================================================
   CONFIG  ·  CẤU HÌNH
   ⚠️ BÀI TẬP 7 — API key dưới đây đang bị "hardcode" (ghi cứng) ngay trong code.
      Ai mở source cũng đọc được → KHÔNG an toàn. Trang sẽ HIỆN key này lên màn
      hình kèm cảnh báo đỏ "key đang bị lộ".

   ⚠️ EXERCISE 7 — the API key below is "hardcoded" right inside the code.
      Anyone reading the source can see it → NOT safe. The page will SHOW this key
      on screen with a red "key exposed" warning.

   Nhiệm vụ / Task: nhờ AI chuyển key ra file .env, thêm .env vào .gitignore, rồi
   khai báo lại trong Environment Variables trên Vercel. Khi key KHÔNG còn nằm
   cứng ở đây nữa, cảnh báo đỏ sẽ tự đổi thành trạng thái xanh "có thể đã an toàn".
   (Đây là key giả định để luyện tập — không phải key thật.)
   =========================================================================== */

const API_KEY = "sk-live-9f3K7pQ2mZx8VbN1aL4tR6yW0eU5cD2h";
